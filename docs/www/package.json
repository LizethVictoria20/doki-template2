{
  "name": "principal-page-doki-template",
  "version": "0.1.0",
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1",
    "format": "prettier --write \"src/**/*.js\"",
    "lint": "prettier --check \"src/**/*.js\""
  },
  "dependencies": {
    "autoprefixer": "^10.2.4",
    "canvas-confetti": "^1.2.0",
    "postcss": "^8.2.6",
    "tailwindcss": "^2.0.3"
  },
  "devDependencies": {
    "prettier": "^2.0.5",
    "snowpack": "^3.0.1"
  }
}
